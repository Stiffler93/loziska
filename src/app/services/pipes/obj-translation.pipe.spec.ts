import { ObjTranslationPipe } from './obj-translation.pipe';

describe('ObjTranslationPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjTranslationPipe();
    expect(pipe).toBeTruthy();
  });
});
